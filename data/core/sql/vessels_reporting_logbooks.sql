SELECT DISTINCT ft.REG_VESSEL_ID as ID, v.REGISTRATION_NUMBER, v.NAME FROM dt_fishing_trip as ft 
LEFT JOIN reg_vessels as v ON v.ID = ft.REG_VESSEL_ID 
WHERE v.CL_APP_VESSEL_STAT_TYPE_ID = 2 ORDER BY v.NAME